apiVersion: v1
kind: Secret
metadata:
  name: demo-secret
  namespace: gitops-demo
  labels: { app: demo-web }
type: Opaque
stringData:
  # Fake values for demo only — replace in real usage
  API_KEY: "demo-not-a-real-key"
